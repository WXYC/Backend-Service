{
  "name": "@wxyc/database",
  "version": "1.0.0",
  "description": "WXYC Database Schema and Client",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf ./dist/*",
    "test": "echo 'No tests defined for database package' && exit 0"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "drizzle-orm": "^0.44.3",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "tsup": "^8.4.0",
    "typescript": "^5.6.2"
  }
}
