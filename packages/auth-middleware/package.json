{
  "name": "@wxyc/auth-middleware",
  "version": "1.0.0",
  "description": "WXYC Authentication Middleware (Cognito-based)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf ./dist/*",
    "test": "echo 'No tests defined for auth-middleware package' && exit 0"
  },
  "dependencies": {
    "aws-jwt-verify": "^5.1.0",
    "express": "^5.1.0",
    "@wxyc/shared": "file:../shared"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.1.0",
    "tsup": "^8.4.0",
    "typescript": "^5.6.2"
  }
}
