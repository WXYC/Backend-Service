{
  "name": "@wxyc/auth-middleware",
  "version": "1.0.0",
  "description": "WXYC Better Auth Middleware for Backend Services",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf ./dist/*",
    "test": "echo 'No tests defined for auth-middleware package' && exit 0"
  },
  "dependencies": {
    "better-auth": "^1.3.23",
    "express": "^5.1.0",
    "@wxyc/shared": "file:../shared"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "tsup": "^8.4.0",
    "typescript": "^5.6.2"
  }
}
