{
  "name": "@wxyc/shared",
  "version": "1.0.0",
  "description": "WXYC Shared Types and Utilities",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf ./dist/*",
    "test": "echo 'No tests defined for shared package' && exit 0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.6.2"
  }
}
