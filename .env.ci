# CI Environment Overrides
# These values override .env for CI testing
# Ports match the docker-compose CI profile mappings
PORT=8081
DB_HOST=localhost
DB_PORT=5433
BETTER_AUTH_URL=http://localhost:8083/auth
AUTH_BYPASS=false

AUTH_USERNAME=test_dj1
AUTH_PASSWORD=testpassword123
