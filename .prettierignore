dist
coverage
node_modules
.cache
.claude
shared/database/src/migrations
*.sql
audit
# Prettier has no Python parser; directory-expansion (`--check .`) already
# skips .py silently, but glob-form invocations error on them (BS#1522).
*.py

# Byte-exact wire golden (BS#2103): the file IS `JSON.stringify(payload, null, 2)`
# from playlist-proxy-wire-golden.test.ts, and wxyc-ios-64 holds a copy pinned by
# SHA-256. Prettier collapses short arrays onto one line, which would rewrite the
# bytes the test exists to pin and break the cross-repo hash. Formatting here is
# the serializer's to define, not the formatter's.
tests/fixtures/recent-entries-v2-wire-golden.json


# Generated by `jobs/library-label-backfill/job.ts` (BS#2669). Same rule as the
# wire golden above: the bytes are the generator's to define, so re-running the
# job after a Prettier pass would reintroduce a diff every time. The .tsv files
# have no Prettier parser and are listed for intent, not effect.
jobs/library-label-backfill/REPORT.md
jobs/library-label-backfill/*.tsv
