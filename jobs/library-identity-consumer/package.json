{
  "name": "@wxyc/library-identity-consumer",
  "job-type": "one-shot",
  "version": "1.0.0",
  "description": "WXYC one-shot ETL: consume LML's bulk-resolve-libraries endpoint and UPSERT verdicts into library_identity + library_identity_source (BS#802, post-#800 cross-cache-identity pivot — Backend is thin-writer, LML is sole composer).",
  "type": "module",
  "main": "./dist/job.js",
  "scripts": {
    "start": "node dist/job.js",
    "build": "tsup --minify",
    "clean": "rm -rf dist",
    "docker:build": "docker build -t wxyc_library_identity_consumer:ci -f ../../Dockerfile.library-identity-consumer ../../",
    "dev": "tsup --watch"
  },
  "license": "MIT",
  "dependencies": {
    "@sentry/node": "^10.56.0",
    "@wxyc/database": "^1.0.0"
  },
  "peerDependencies": {
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
