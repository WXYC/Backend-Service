{
  "name": "@wxyc/flowsheet-metadata-backfill",
  "cron-schedule": "10 * * * *",
  "version": "1.0.0",
  "description": "WXYC Epic C C6 (BS#895) hourly gap-recovery sweep: enrich flowsheet track rows the CDC enrichment consumer (apps/enrichment-worker) missed (metadata_status='pending', past a 15-min consumer grace window, inside a 6h recovery-window ceiling), plus the epic #1810 W4 rotation self-heal pass. Formerly nightly 06:00 UTC (#631 / #638 / #641), then gated off entirely while BS#1631 and the BS#1591 play-priority drain landed; orchestrator's cooperative pause defers when DJs are active.",
  "type": "module",
  "main": "./dist/job.js",
  "scripts": {
    "start": "node dist/job.js",
    "build": "tsup --minify",
    "clean": "rm -rf dist",
    "docker:build": "docker build -t wxyc_flowsheet_metadata_backfill:ci -f ../../Dockerfile.flowsheet-metadata-backfill ../../",
    "dev": "tsup --watch"
  },
  "license": "MIT",
  "dependencies": {
    "@sentry/node": "^10.69.0",
    "@wxyc/database": "^1.0.0",
    "@wxyc/lml-client": "^1.0.0",
    "@wxyc/metadata": "^1.0.0"
  },
  "peerDependencies": {
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
