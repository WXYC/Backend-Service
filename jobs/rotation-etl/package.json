{
  "name": "@wxyc/rotation-etl",
  "cron-schedule": "*/30 * * * *",
  "version": "1.0.0",
  "description": "WXYC Rotation ETL: Import and sync rotation data from tubafrenzy",
  "type": "module",
  "main": "./dist/job.js",
  "scripts": {
    "start": "node dist/job.js",
    "start:poll": "node dist/job.js --poll",
    "build": "tsup --minify",
    "clean": "rm -rf dist",
    "docker:build": "docker build -t wxyc_rotation_etl:ci -f ../../Dockerfile.rotation-etl ../../",
    "dev": "tsup --watch"
  },
  "license": "MIT",
  "dependencies": {
    "@wxyc/database": "^1.0.0"
  },
  "peerDependencies": {
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
