{
  "name": "@wxyc/library-etl",
  "job-type": "one-shot",
  "version": "1.0.0",
  "description": "WXYC Legacy Library ETL Job",
  "type": "module",
  "main": "./dist/job.js",
  "scripts": {
    "start": "node dist/job.js",
    "build": "tsup --minify",
    "clean": "rm -rf dist",
    "docker:build": "docker build -t wxyc_library_etl:ci -f ../../Dockerfile.library-etl ../../",
    "dev": "tsup --watch"
  },
  "author": "Adrian Bruno",
  "license": "PolyForm-Noncommercial-1.0.0",
  "dependencies": {
    "@wxyc/database": "^1.0.0"
  },
  "peerDependencies": {
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
