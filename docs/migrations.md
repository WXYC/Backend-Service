# Drizzle Migrations

Operational rules for adding migrations to `shared/database/src/migrations/`. CLAUDE.md links here.

## Quick reference

```bash
npm run drizzle:generate   # Generate SQL migration from schema changes
npm run drizzle:migrate    # Apply migrations to database
npm run drizzle:drop       # Delete a migration file
```

Each migration produces three artifacts: the `.sql` file, a journal entry in `meta/_journal.json`, and a per-migration snapshot in `meta/<idx>_snapshot.json`. Always run `drizzle:generate` to add a migration — never hand-create the journal entry, never hand-edit the SQL or snapshot.

## Dev/CI Postgres version vs. prod RDS (read before writing DDL)

<!-- @rule id=dev-prod-pg-version-skew enforced-by=none added=2026-07-28 incidents=#1424 -->

**Dev, CI, and e2e Postgres containers (`dev_env/docker-compose.yml`, `.github/workflows/test.yml`) pin `postgres:18.0-alpine`. Production RDS runs PostgreSQL 14.22.** This is a known, currently-intentional skew (BS#1424), not a drift bug — don't "fix" it by silently bumping or downgrading either side; that's a separate decision the maintainers haven't made yet (see BS#1424 for the tradeoffs).

The consequence: PG15+-only syntax (`NULLS NOT DISTINCT`, `MERGE`, etc.) compiles fine against the dev container and passes the unit/integration suites, but real prod Postgres rejects it. The only CI job that actually validates a migration against prod's PG major is **`Migration Dry-Run (prod-shaped data)`** (`migrate-dryrun` in `.github/workflows/test.yml`) — it restores a real RDS snapshot and runs the pending migrations against it. That job is where a PG15+-only migration fails (`42601 syntax error`), not lint/typecheck/unit/integration. See BS#1424 for the discovery case: PR #1414 first attempted `NULLS NOT DISTINCT` (PG15+ syntax), the dry-run job caught it, and the fix was a PG14-compatible partial-index pair.

**When authoring a migration, target PG14 syntax** even though the dev container will happily run newer syntax — the dev container is not a preview of what prod will accept. When in doubt, check the [PostgreSQL 14 release notes](https://www.postgresql.org/docs/14/release-14.html) (what's guaranteed available) against the [PG15](https://www.postgresql.org/docs/15/release-15.html)+ notes (what's new and unavailable in prod) before relying on a feature.

If prod RDS is ever upgraded to PG15+, update this note and re-pin the dev/CI/e2e containers to match.

## One-time per-clone setup

`shared/database/src/migrations/meta/_journal.json` is an append-only Drizzle index. Every PR that adds a migration appends an entry; concurrent PRs collide on the array even when the new entries don't overlap. The repo ships `.gitattributes` with `merge=journal`, but the actual merge driver lives in `.git/config` and isn't checked in — each collaborator must register it once after cloning:

```bash
npx git-merge-append install \
  --name journal \
  --array-path entries --key idx --sort-by idx \
  -- shared/database/src/migrations/meta/_journal.json
```

After this, `git rebase` / `git merge` resolve concurrent journal appends automatically. If you skipped the install and are mid-rebase with `<<<<<<<` in `_journal.json`, run `npx git-merge-append resolve --array-path entries --key idx --sort-by idx -- shared/database/src/migrations/meta/_journal.json` to fix it post-hoc. See [git-merge-append](https://github.com/jakebromberg/git-merge-append) for details.

## Rules

<!-- @rule id=journal-snapshot-coupling enforced-by=scripts/validate-migrations.mjs:check-7 added=2026-02-15 incidents=#590 -->

**Always run `drizzle:generate`.** The `meta/` directory holds two parallel artifacts: `_journal.json` (one entry per migration, ordered by idx) and per-migration `meta/<idx>_snapshot.json` files (each capturing cumulative schema state through that idx). Drizzle-kit emits all three (SQL, journal entry, snapshot) as a side-effect of `generate`. Hand-creating a journal entry without running `generate` ships the migration but skips the snapshot, and the chain rots silently — `drizzle:generate` then diffs against the last _real_ snapshot, producing a noisy catch-up migration that contains operations the database already has. WXYC/Backend-Service#590 covers the cleanup. `scripts/validate-migrations.mjs` Check 7 enforces the rule going forward via the `HISTORICAL_MISSING_SNAPSHOT_IDXS` allowlist (which must not grow).

<!-- @rule id=hand-edit-when enforced-by=scripts/validate-migrations.mjs:check-1 added=2026-02-01 incidents=#400,#550 -->

**The one sanctioned hand-edit on the new journal entry is the `when` field.** Drizzle-kit auto-stamps `when = Date.now()`, but Drizzle's runtime migrator (`drizzle-orm/pg-core/dialect.js`) decides what to apply by comparing each entry's `when` against `max(__drizzle_migrations.created_at)` in production — anything `when <= max` is silently skipped on every subsequent migrate run, with no way to re-insert (see #400 / #550 for the two production incidents this caused). Once a previous PR landed a future-dated `when` (deliberately or via a now-stale fake clock), every subsequent migration's auto-stamped `when` will be below that cursor and must be bumped. **The recipe is: set the new entry's `when` to `previous_entry.when + 1` (one millisecond above the journal tail).** PR #551 did this explicitly when replaying 0054 as 0065 (`when=1779683200001`); PR #564 used `1779683200002` for 0066; the practice continues through 0067/0068. Validator Check 1 enforces strict monotonicity, so any deviation from `+1ms` is fine as long as it's strictly greater. Do not touch any other journal field by hand, and do not edit the snapshot — drizzle-kit uses snapshots as the diff baseline, so any hand-edit there rots the chain.

<!-- @rule id=parallel-pr-when-collision enforced-by=none added=2026-03-12 incidents=#400,#550 -->

**Parallel-PR collision on `when`.** Two open PRs picking the same `previous_entry.when + 1` would each generate a journal entry at identical `when`. The `git-merge-append` driver resolves _structural_ conflicts on the entries array (concurrent appends), but it does not detect that two entries carry the same `when` value — both can land cleanly, and Drizzle's runtime cursor would silently skip the second on first deploy (the same #400 / #550 failure mode this rule exists to prevent). When the journal-merge driver auto-resolves your branch and the resulting tail has a duplicate `when`, the second-merging PR must rebase and bump again before merging.

<!-- @rule id=sql-comment-block enforced-by=none added=2026-02-15 -->

**The generated SQL file may grow a leading comment block.** Drizzle-kit emits the bare DDL; the established practice (see 0053, 0063, 0065) is to prepend a `--`-comment header explaining the migration's purpose, the operational caveats (lock behavior, expected duration), and any companion backfill job. The DDL itself stays exactly as drizzle-kit produced it.

<!-- @rule id=if-not-exists-index enforced-by=none added=2026-03-01 -->

**`CREATE INDEX` migrations may add `IF NOT EXISTS` by hand.** When an index ships against a large prod table, the deploy runbook is to build it CONCURRENTLY out-of-band first (no AccessExclusiveLock, no INSERT pause) and then merge a migration that finds it already there. Drizzle-kit doesn't emit `IF NOT EXISTS` for indexes; hand-edit it onto the `CREATE INDEX` line so the migration is a no-op against the prod DB while fresh dev databases pick the index up on first migrate. The comment block must include the exact `CREATE INDEX CONCURRENTLY ...` command for ops to run, and must explain that the in-migration form is _not_ CONCURRENTLY because the migration runs inside a transaction (`CONCURRENTLY cannot run inside a transaction block`) — one shared by every pending migration, not one per file, per the `single-transaction-migrate` rule below; the conclusion here is unaffected either way. Reference: 0057, 0068, 0070. Don't add `IF NOT EXISTS` to other DDL (ALTER TABLE, etc.) — the index pattern is the only one that needs prod pre-prep.

<!-- @rule id=ddl-only enforced-by=none added=2026-02-15 incidents=#511 -->

**Migrations are DDL-only.** Bulk DML (rewrites of more than ~10k rows) does not belong inside a migration file because the DDL portion takes an `AccessExclusiveLock` that is held until the transaction commits, and a long DML can wedge the table for hours. Put the rewrite in a one-shot backfill job under `jobs/<name>-backfill/` (declared with `"job-type": "one-shot"` in `package.json`). The build pipeline pushes the image to ECR; a human invokes it via `docker run --rm --env-file .env <image>` during a low-traffic window. If a downstream migration depends on the backfill having run, gate it with a `DO $$ ... RAISE EXCEPTION ... END $$;` precondition guard at the top of the file. See `0053_flowsheet-dj-name-column.sql` + `jobs/flowsheet-dj-name-backfill/` + `0054_flowsheet-search-doc-with-dj-name.sql` for the canonical pattern, and issue #511 for the incident this rule was learned from.

<!-- @rule id=post-bulk-update-analyze enforced-by=scripts/check-bulk-update-analyze.mjs added=2026-05-15 incidents=#934 -->

**Bulk UPDATEs require a paired ANALYZE on the touched tables.** After a `.sql` file rewrites rows on a heavily-indexed table (`flowsheet`, `library`, `rotation`, `artists`), the planner's stats on the touched columns are stale until autovacuum runs. Queries that _should_ use the GIN trigram / partial / functional indexes covering those columns can revert to a sequential or bitmap-heap scan; for the dj-site `/flowsheet/suggest/*` autocomplete endpoints, that surfaces as Express 5 s timeouts in front of on-air DJs (the 2026-05-15 regression in BS#934). The fix is always to follow the UPDATE with an explicit `ANALYZE <table>;` for each table the UPDATE touched. `ANALYZE` **can** run inside a transaction (it is `VACUUM` that cannot — verified on PG14.22), so it can live at the bottom of the migration itself; an operator-run script is only needed when the rewrite happens out of band. Migration `0150` does exactly this, and needs to: `ALTER COLUMN ... TYPE` drops the column's `pg_stats` row and bumps no counters, so autoanalyze never repairs it. A bare `ANALYZE;` re-stats every table the role can see and is the simplest form when more than 2-3 tables are involved. The check at `scripts/check-bulk-update-analyze.mjs` greps every `.sql` file under `shared/database/src/migrations/`, `scripts/`, and `jobs/` and warns on any `UPDATE table SET ...` that isn't paired with an `ANALYZE` (or a bare `ANALYZE;`). Suppress with a `-- @no-analyze-needed: <reason>` comment when the UPDATE is small enough that stats drift doesn't matter — single-row config UPDATEs, fresh-table backfills, etc. Already-applied migration files whose hashes are frozen in `meta/applied-hashes.json` use the per-tag `HISTORICAL_NO_ANALYZE_NEEDED_TAGS` allowlist in the script (a comment retroactively added would break Check 11 of `validate-migrations.mjs`). See [`bulk-update-playbook.md`](bulk-update-playbook.md) for the full operational pattern (per-row cost, async-commit knob, infinite-loop pitfall, sync-gap remediation).

<!-- @rule id=constraint-precondition-guards enforced-by=scripts/validate-migrations.mjs:check-8 added=2026-04-20 incidents=#705 -->

**Constraint-adding migrations should include precondition guards.** Any migration that adds a `UNIQUE`, `CHECK`, `NOT NULL`, or `FOREIGN KEY` constraint depends on a data invariant that current rows must satisfy. If they don't, Postgres aborts the migration mid-apply and the deploy wedges (recovery pattern: #511). Guard the DDL with a `DO $$ ... RAISE EXCEPTION ... END $$;` block above the `CREATE`/`ALTER` so the migration fails fast with a readable message and the transaction rolls back cleanly. Example for the rotation unique partial index from #694:

```sql
-- 0071 unique partial index on (rotation.album_id, rotation.rotation_bin) WHERE kill_date IS NULL
-- Requires: all duplicate active groups must be resolved first.

DO $$
DECLARE dup_count int;
BEGIN
  SELECT COUNT(*) INTO dup_count
  FROM (
    SELECT album_id, rotation_bin
    FROM wxyc_schema.rotation
    WHERE kill_date IS NULL
    GROUP BY album_id, rotation_bin
    HAVING COUNT(*) > 1
  ) g;
  IF dup_count > 0 THEN
    RAISE EXCEPTION 'Cannot apply rotation_active_album_bin_uniq: % duplicate groups remain. Run rotation-dedupe job first or pre-clean manually.', dup_count;
  END IF;
END $$;

-- The actual DDL follows
CREATE UNIQUE INDEX IF NOT EXISTS ...
```

The same shape covers `NOT NULL` (count `WHERE col IS NULL`), `CHECK` (count rows that violate the predicate), and `FK` (count orphans via `LEFT JOIN ... WHERE referenced.id IS NULL`). The pattern is the same prevention this codebase already uses on the 0053 + `jobs/flowsheet-dj-name-backfill/` + 0054 chain — generalize it to any constraint-adding migration. Some constraints are provably safe (e.g. a `UNIQUE` index on a freshly-added nullable column, or `NOT NULL` paired with a `DEFAULT`); when no real precondition exists, document the reasoning with a `-- @no-precondition-needed: <reason>` comment so the linter (`scripts/validate-migrations.mjs` Check 8) suppresses its warning. That annotation is for migrations not yet applied; for one already applied, the rationale goes in `shared/database/src/migrations/PRECONDITION_NOTES.md` instead, because retrofitting the comment would change the file's SHA-256 and trip Check 11 (this is the #705 wedge). The PR-bot data-shape report (companion #703) catches violations at PR time; the precondition guard is the last line of defense at apply time.

<!-- @rule id=cross-cache-identity-guards enforced-by=scripts/check-precondition-guards.sh added=2026-04-25 -->

**Cross-cache-identity precondition guards (cross-epic, project-scoped).** The precondition-guard pattern above (0053 → `jobs/flowsheet-dj-name-backfill/` → 0054, scoped within a single migration chain) extends to **cross-epic** prerequisite chains for the cross-cache-identity project. The substrate migration `0NNN_library_identity_substrate.sql` (filed under WXYC/Backend-Service#663; PR number to be backfilled here when the substrate PR opens) ships its gate-check at `scripts/check-library-identity-gate.sql`. Any migration in any epic that FK-references `library_identity` / `library_identity_source` / `library_identity_history`, or adds a `NOT NULL` / `UNIQUE` / `CHECK` constraint to those tables, must include a `DO $$ ... RAISE EXCEPTION ... END $$;` block that calls or inlines the gate-check's `truly_unresolved_rows < 1000` predicate. Same mechanism as 0053/0054, scoped across epics rather than within one chain. Plan reference: `WXYC/wiki/plans/library-hook-canonicalization-plan.md` §3.2.3. CI enforcement is the `Migration guards` job in `.github/workflows/test.yml`, which runs `scripts/check-precondition-guards.sh` (ships with the substrate PR); a migration that legitimately doesn't need the guard opts out with a `-- precondition-guard: not-required (rationale)` first line.

<!-- @rule id=decision-timeout-flag-only enforced-by=none added=2026-04-25 -->

**Decision-timeout pattern (cross-cache-identity scope).** For decision timeouts during the cross-cache-identity project, use **flag-only** GitHub Action comments per plan §4 step 0. The workflow lives at `WXYC/catalog-audits/.github/workflows/cross-cache-identity-timeouts.yml` (filed in WXYC/Backend-Service#666). It posts a deadline-overdue comment after 10 business days but does NOT propose or set a default — silence does not select an option. The pattern is intentionally project-scoped: do not reuse it for other projects without explicit similar documentation in their plan. The reason it cannot generalize is that auto-defaulting on silence violates the "questions are not commands" principle (`~/.claude/CLAUDE.md`); flag-only is the form that informs without acting. Forward link: filter-decision gate is tracked at WXYC/catalog-audits#11 (E5 step 1 — library coverage filter discovery).

<!-- @rule id=library-identity-source-set enforced-by=scripts/validate-library-identity-sources.sh added=2026-04-25 -->

**`library_identity_source` source-set extensibility (manual-override + dual-table writer).** The `library_identity_source.source` column is an open enum: 8 sources today (`discogs_master`, `discogs_release`, `mb_release_group`, `mb_release`, `mb_recording`, `wikidata`, `spotify`, `apple_music`) per plan §3.2.2. The set is hardcoded in three places that must move in lockstep: (a) `shared/database/src/schema.ts` (`library_identity` per-source columns), (b) `jobs/library-identity-manual-override/` (validator's `failure_reason` enum + CSV schema), (c) `apps/backend/services/identity/*` (writer's source iteration list). Adding a 9th source (e.g., a Bandcamp leg per LML#207) is a single coordinated PR that updates all three; the `lint:identity-sources` CI job (script `scripts/validate-library-identity-sources.sh`, ships with the substrate PR) fails on mismatch so no half-wired source can land. Plan reference: §3.2.4 manual-override workflow extensibility.

<!-- @rule id=flag-authoritative-backfill enforced-by=none added=2026-08-01 incidents=#1451 -->

**A backfill predicate for a completeness/status flag must match the app's authoritative rule for that flag, not invent its own.** Migration 0043's `has_completed_onboarding` backfill additionally required a non-empty `dj_name`, but dj-site treats `dj_name` as optional and only requires `real_name` — the two predicates silently diverged and stranded ~17 legacy DJs at `/login?incomplete=true` (BS#1451). When a migration backfills a flag that an app-layer guard also reads, derive the backfill `WHERE` clause from the same source the guard uses (or read the guard's code before writing the predicate), and prefer never re-deriving a flag from profile fields at all once the flag itself is authoritative — see `docs/authentication.md`'s onboarding-completeness note.

<!-- @rule id=single-transaction-migrate enforced-by=none added=2026-08-17 incidents=#2174 -->

**`migrate()` applies every pending migration in ONE transaction, so a migration is not isolated from its predecessors on a fresh database.** Both `dev_env/init-db.mjs` (`runMigrations`) and `scripts/dryrun-migrate.mjs` call drizzle-orm's programmatic `migrate()` from `drizzle-orm/postgres-js/migrator` directly rather than shelling out to the `drizzle-kit migrate` CLI, and that function wraps the whole batch of pending migrations in a single transaction — not one transaction per file. The CLI is not an escape from it: `drizzle-kit migrate` — what the Quick reference's `npm run drizzle:migrate` runs — imports that same `drizzle-orm` migrator, so every path that applies a migration in this repo shares the one transaction. Note that `init-db.mjs` is not a dev-only script despite its path: `Dockerfile.migrate` copies it and runs `CMD ["node", "init-db.mjs"]`, and `deploy-base.yml` builds that image for the deploy's migrate job, so this is the production runner too. Two consequences follow. First, **an enum value added by `ALTER TYPE ... ADD VALUE` cannot be _used_ by a later migration on a fresh database**, because both migrations land in the same transaction and Postgres rejects the reference with `unsafe use of new value "X" of enum type Y`. A `CREATE TYPE` earlier in the same batch does not exempt it: Postgres rejects a value added by `ALTER TYPE ... ADD VALUE` even when the enum type itself was created earlier in that same transaction (measured on 14.24), and only a value present in the original `CREATE TYPE` is usable within the transaction that created it — the boundary condition 0112's header got wrong. Compare via `column::text = 'X'` instead of a bare `column = 'X'` or its explicit `column = 'X'::the_enum_type` form — both of those construct the enum value (the bare literal is coerced to the column's type), while the text cast never does, so it never trips the restriction. That remedy covers **comparisons only** — do not generalize it to writes. There is no in-transaction escape for _writing_ the new value: `SET col = 'X'`, `SET col = 'X'::the_enum_type`, and `SET col = 'X'::text::the_enum_type` all raise the same error (measured on 14.24), as would a `DEFAULT` or `CHECK` naming it, because a write has to construct the value and that is precisely what is forbidden. Nor can a migration file write the value by waiting for a later deploy: that fixes only the production axis, because a fresh database has the `ADD VALUE` and the write pending together however many deploys separate them — so a migration written that way is red on `Integration-Tests`, on every new clone, and on every `npm run db:start`, permanently. The write has to land after the migration transaction commits — at runtime from app code, or from a post-deploy one-shot backfill job per the `ddl-only` rule above — which is the reasoning 0094's header already sets out. 0150 is clear of this because the bins it writes (`S`/`H`/`L`) are all in 0000's original `CREATE TYPE`; only its `'N'` reads need the `::text` form. Second, and more generally, **any "an earlier migration already created/added this" assumption that Postgres gates on transaction boundaries holds only where that earlier migration is genuinely already committed** — which is a property of what is pending, not of which environment you are in. A fresh database has every migration pending at once, so nothing an earlier file did is committed yet when a later file in the same run touches it. Production is usually the opposite case, but not reliably: `docs/deploy.md`'s `deploy-cadence-24h` rule records the 2026-05-04 wedge where four days of accumulated migrations left 0071, 0072, and 0073 stacked unapplied — that incident predates auto-deploy, and the rule names a silent or failed auto-deploy as the way the same stacking recurs today — and any deploy carrying two such migrations runs them in one transaction exactly like a fresh database would. Write the migration so it does not depend on the boundary at all, rather than reasoning about which environment is safe.

**The failure is invisible to the path that looks highest-stakes.** Against production, and against the `Migration Dry-Run (prod-shaped data)` job's restored RDS snapshot, the earlier migration committed long ago — only the new file is pending, the bare form works, and the dry-run passes. It breaks only on a genuinely _fresh_ database: CI's `Integration-Tests` job, a new clone, a local `npm run db:start`. That inverts the usual intuition that the dry-run is the stricter gate. Be precise about the blind spot, because it is narrower than "the dry-run can't see enum misuse": the dry-run applies exactly the migrations still pending against a prod snapshot, so it _does_ catch an `ALTER TYPE ... ADD VALUE` and a consumer of that value introduced in the same PR — both are pending there too, inside one transaction. What it cannot see is the **cross-deploy** case, where the predecessor committed in an earlier deploy and so is not pending against prod at all, while a fresh database still has it pending alongside the new file. For that case `Integration-Tests` is the only job that catches it. Worked example: PR #2174 / migration `0150_reclassify-and-drop-fictional-n-bin.sql`, where a bare `rotation_bin = 'N'` comparison passed the dry-run and failed `Integration-Tests` — `freq_enum` is created in 0000 and gains `'N'` in 0041, both long committed in prod but both still pending on a fresh database; the fix is the `rotation_bin::text = 'N'` form, and the migration's own header comment records the failure mode inline. Every migration that has ever added a value to `discogs_release_id_source_enum` or `concert_source_enum` writes this restriction up — see 0086, 0094, 0109, 0112, and 0117 — but three of them predate this rule, reason wrongly about it, and are frozen post-apply, so don't trust their prose verbatim: 0117 and 0094 state the transaction model wrong, while 0112 states it correctly and errs only in the fresh-database inference it draws. Read the per-file corrections in `shared/database/src/migrations/PRECONDITION_NOTES.md`'s Transaction-model corrections section instead. 0086 and 0109 predate this rule too but scope their claims to their own file and are fine as written. Splitting an `ALTER TYPE ... ADD VALUE` into its own migration is still worth doing — it keeps the value committed for every _later deploy_ — but it is not a guarantee for the deploy that introduces it, and never for a fresh database.

**Verify locally before trusting the dry-run.** Applying migration files one at a time through `psql` autocommits between files and cannot reproduce this — the check needs every pending migration inside one explicit `BEGIN`/`COMMIT`, matching what `migrate()` actually does:

```bash
{ echo 'CREATE EXTENSION IF NOT EXISTS pg_trgm;'
  echo 'BEGIN;'
  for tag in $(jq -r '.entries[].tag' shared/database/src/migrations/meta/_journal.json); do
    cat "shared/database/src/migrations/$tag.sql"; echo ';'
  done
  echo 'COMMIT;'; } | psql -v ON_ERROR_STOP=1 -d <fresh-db>
```

The `CREATE EXTENSION` line is not optional. `init-db.mjs` installs `pg_trgm` (via `dev_env/install_extensions.sql`) before it calls `runMigrations()`, and ten migrations from 0018 on build `gin_trgm_ops` indexes, so a bare container aborts at 0018 on `operator class "gin_trgm_ops" does not exist for access method "gin"` — long before reaching the enum boundary this check exists to exercise. It sits ahead of the `BEGIN` because it is setup, not part of what is under test. (`scripts/dryrun-migrate.mjs` skips extension installation for the opposite reason: its RDS snapshot already has them.)

Pair it with the PG14-vs-PG18 guidance above (BS#1424): prod RDS is 14.22, so run this against a `postgres:14` container, not the `postgres:18.0-alpine` dev/CI image — a PG18-only fresh-database check can pass while still failing on real prod syntax and transaction semantics. A disposable PG14 instance for the check: `docker run --rm -e POSTGRES_PASSWORD=postgres -p 5433:5432 postgres:14`, then substitute `PGPASSWORD=postgres psql -v ON_ERROR_STOP=1 -h localhost -p 5433 -U postgres -d postgres` for the `psql` invocation above. Both additions are load-bearing: a fresh container ships only `postgres`/`template0`/`template1`, so `-d postgres` (already empty) is the fresh database rather than a name you invent, and `PGPASSWORD` is required because stdin is the migration stream — the password prompt `psql` falls back to goes unanswered in a non-interactive shell and the connection dies with `password authentication failed`.

## Attempt-at markers

Eight `timestamp with time zone` markers record "this row was attempted by job X" so subsequent passes can target only the rows that still need work, without confusing tried-and-no-match for tried-and-failed. The shared shape: stamped on responded outcomes (success-with-match OR success-no-match), left NULL on transient failures (the `.catch` arm deliberately omits the write), read by a cron or warm walker that re-attempts the NULL set. Individual bullets call out deviations from that shape.

- `flowsheet.legacy_link_attempted_at` — set when the `legacy_release_id → library.id` resolver ran for the row and could not link. Migration 0063, populated by `jobs/broken-fk-recovery`. Lets B-2.2's LML backfill query both never-had-legacy-id rows and the broken-FK residual in one predicate.
- `flowsheet.metadata_attempt_at` — **historical marker only as of BS#895 (Epic C C6, migration 0132).** Set when `jobs/flowsheet-metadata-backfill` (the only remaining writer) responded for the row (success-with-match OR success-no-match) via `enrich.ts:applyEnrichment`/`stampDeadLetter`. Migration 0069 (#639); originally also stamped at runtime by `apps/backend/services/metadata/enrichment.service.ts`, but that fire-and-forget path's call sites were deleted in Epic C C5 (#894) and the module is now unreachable dead code (not yet removed). Control flow moved to `metadata_status` (BS#891, migration 0078) when BS#895 flipped the cron's sweep predicate from `metadata_attempt_at IS NULL` to `metadata_status = 'pending'` — see `worklist.ts`'s module docstring for the full predicate-swap design (the grace-window + recovery-window-ceiling story that lets the new predicate ignore the undrained historical backlog #1011 left behind (≈163k rows as of 2026-08-26, down from ~748k after the 2026-08-01→08-12 catch-up drain (BS#2279))). This column now exists only for the other jobs still keyed on it as an audit/completeness signal: `jobs/album-metadata-backfill`'s `verifyComplete` (`metadata_attempt_at IS NOT NULL`) and its backing index — `flowsheet_album_id_enriched_idx` until BS#2032 replaced it with `flowsheet_album_id_linked_idx` (migration 0139), which carries `metadata_attempt_at` as a stored key rather than in the partial predicate so that query keeps its Index Only Scan while a bare `album_id = <id>` equality also becomes index-served. It is ALSO the timestamp the epic #1810 W4 self-heal state-change gate compares against `rotation.discogs_release_id_resolve_attempted_at` (below) to detect a NULL→present transition without a blind re-scan — see `worklist.ts:buildRotationSelfHealCandidates`. Backfill recipe (historical BS#891 state-derivation, now applied) at [`docs/flowsheet-metadata-status-backfill.md`](flowsheet-metadata-status-backfill.md).
- `concerts.artist_resolve_attempted_at` — set when LML's bare-name artist resolve (LML#759 `POST /api/v1/artists/resolve/bulk`) RESPONDED for the row's headliner name: resolved, `ambiguous`, or `not_found`. Migration 0116 (BS#1614), stamped by `jobs/concerts-artist-lml-resolver/` (`targets.ts`). Deliberately NOT stamped on `escalation_unavailable` verdicts or transport errors — LML#759 defines that verdict as "couldn't ask," not "asked and missed," so those rows must stay immediately retryable rather than sit behind the no-match TTL. The job re-attempts `artist_resolve_attempted_at IS NULL` rows every run and stamped no-match rows past `CONCERTS_ARTIST_RESOLVE_NO_MATCH_TTL_DAYS` (default 30). **Load-bearing dependency: the "stay retryable" guarantee only holds because the job runs nightly (`35 5 * * *` UTC via `package.json` `cron-schedule`) — there is no other pass over the NULL set.** Candidates are upcoming-only (`starts_on >= (now() AT TIME ZONE 'America/New_York')::date` — the venue-local Eastern date the read path windows on, not server-clock `CURRENT_DATE`), so past shows age out of the retry population without a stamp.
- `rotation.discogs_release_id_resolve_attempted_at` — set when the recurring rotation release-id resolver receives a definitive LML response for an active row whose `discogs_release_id` is NULL: trusted `direct` match, `no_match`, trust-gated fallback, or rejected `<= 0` sentinel. Migration 0131 (BS#1813), stamped by `jobs/rotation-release-id-backfill` (`writer.ts`) under the same `discogs_release_id IS NULL` race guard as the release-id write. Transient LML throws leave it NULL so the row retries on the next cron tick. The job re-attempts NULL markers every run and stamped no-match/trust-rejected/sentinel rows past `ROTATION_RELEASE_ID_NO_MATCH_TTL_DAYS` (default 30). A successful match also writes `discogs_release_id_source='lml_offline_backfill'`; later tubafrenzy paste ids still win via rotation-etl's COALESCE/CASE upsert. **Load-bearing dependency: the retry guarantee holds because the job is cron-registered (`17 */6 * * *` UTC via `package.json` `cron-schedule`) with cooperative live-DJ pause.** Second consumer as of BS#895 / epic #1810 W4: `jobs/flowsheet-metadata-backfill`'s rotation self-heal pass compares this timestamp against `flowsheet.metadata_attempt_at` (above) to detect a `rotation.discogs_release_id` NULL→present transition — a value here strictly AFTER a rotation-linked `enriched_no_match` flowsheet row's last attempt re-selects that row, state-change-gated rather than on a blind timer.
- `flowsheet.no_match_recheck_attempted_at` — set when the recurring cause-agnostic no-match recheck sweep receives a definitive response for a terminal `metadata_status = 'enriched_no_match'` row: a fresh no-match or a trust-gate rejection. Migration 0151 (BS#2176), stamped by `jobs/flowsheet-no-match-recheck` (`writer.ts:markRecheckAttempted`) under the same `metadata_status = 'enriched_no_match'` race guard as the trusted-match write. Transient LML failures (a throw, a cascade-timeout body, or a degraded response with no usable answer) leave it NULL so the row stays eligible. Unlike the sibling markers above, eligible is not the same as re-read on the next run: BS#2218 layered an OFFSET cursor (`cronjob_runs.cursor_position`, migration 0152) over the candidate ordering, so a transient row is re-read when the cursor comes back around to it. The job re-attempts NULL markers every run and stamped no-match/trust-rejected rows past `FLOWSHEET_NO_MATCH_RECHECK_TTL_DAYS` (default 14). **Deliberately NOT a reuse of `flowsheet.metadata_attempt_at` above** — that column's NULL-vs-stamped split is the C6 gap-recovery sweep's load-bearing writer discriminator, and this job's candidate predicate never reads or writes it (pinned by `tests/unit/jobs/flowsheet-metadata-backfill/worklist.test.ts`'s exact-match allowlist on the C6 predicate). **Load-bearing dependency: the retry guarantee holds because the job is cron-registered (`47 */6 * * *` UTC via `package.json` `cron-schedule`) with cooperative live-DJ pause**, structurally the same shape as `rotation.discogs_release_id_resolve_attempted_at` above (its closest sibling).
- `rotation.tracklist_lookup_attempted_at` — set when the rotation-tracks picker's tier-3 LML cascade returned nothing for a row. Migration 0087 (#1234), stamped at runtime by `resolveRotationDiscogsReleaseViaLml` in `apps/backend/services/library.service.ts` on the negative branch (positive outcomes write to the per-process LRU; the persisted column is negative-only). `resolveRotationPickerSource` reads the column and short-circuits the LML call when it's set within `ROTATION_TRACKLIST_LOOKUP_NEGATIVE_WINDOW_MS` (7 days). Transient LML failures `return null` before reaching the write so the column stays at its prior value (NULL on first failure, or a previous stamp if the row has been attempted before) — the row stays eligible for retry. **Load-bearing dependency: re-warm comes from the `rotation-tracks-cache-warm` boot walker, which iterates the same rows on every BS restart; deploys are the implicit cron.** The 7-day window absorbs music-director-typo-correction edits within a week (rotation-etl's upsert preserves the column across ticks). Companion to BS#1230's per-`release_id` LRU and BS#1233's in-memory negative TTL bump.
- `concert_performers.artist_resolve_attempted_at` — binds ONLY the Phase-D LML arm (the offline verify-before-mint resolver for support-act performers absent from the WXYC library — the `concerts.artist_resolve_attempted_at` analog, one level down). Migration 0128 (BS#1759, parent #1618); stamped by `jobs/concerts-artist-lml-resolver/` (`targets.ts`'s `supportTarget`, BS#1763) — the same job and the same `targets.ts` file that stamps `concerts.artist_resolve_attempted_at` for headliners, above. Byte-identical semantics: stamped when LML RESPONDED for the row's `raw_name` (resolved / ambiguous / not_found), left NULL on `escalation_unavailable` verdicts and transport errors so those rows stay immediately retryable. The job re-attempts `artist_resolve_attempted_at IS NULL` rows every run and stamped no-match rows past `CONCERTS_ARTIST_RESOLVE_NO_MATCH_TTL_DAYS` (default 30) — the same TTL knob the headliner target uses, since both targets share one `runResolve` call. The Phase-B pure-SQL strict/alias resolve arm (`jobs/concerts-artist-resolver`) carries **no** marker, matching its treatment of headliners. **Load-bearing dependency: the "stay retryable" guarantee only holds because the job runs nightly (`35 5 * * *` UTC) — there is no other pass over the NULL set.**
- `library.unresolved_attempted_at` — set when LML's `POST /api/v1/identity/bulk-resolve-libraries` RESPONDED with a definitive _no-match_ (`kind: unresolved` or `kind: compilation`) for a library row during the `jobs/library-identity-consumer` BS#974 NULL-canonical expansion. Migration 0130 (BS#974); stamped by `writer.ts:stampUnresolvedAttemptedAt`, called from `orchestrate.ts` per batch. **Two deviations from the shared shape above.** (1) **No-match only** — a `single_artist` resolution is NOT stamped here; its `library_identity` row is the success marker, so the read predicate's `NOT EXISTS(library_identity)` clause already excludes it. Transient failures (`lml_error`/`writer_error`/cardinality/untrusted) stay NULL-retryable as usual. (2) **Manual re-attempt, no backstop** — this job is one-shot (`"job-type": "one-shot"`, invoked via `docker run`, in no cron registration or `docs/ops-cron-scheduling.md`), so unlike the cron/walker-backed markers above, a stamped row is only re-attempted when an operator re-runs the job past `UNRESOLVED_RETRY_DAYS` (default 30 — a _separate_ knob from the 7-day identity-freshness `STALE_THRESHOLD_DAYS`). Read only when `INCLUDE_NULL_CANONICAL=true`; the column is inert (and the marker never stamped) with the flag off, so a deploy is a zero-change no-op until an operator opts in. **BS#801 note:** compilations resolve into per-track tables, not the main `library_identity` row, so `NOT EXISTS(library_identity)` stays true for them and they re-eligible every window (bounded, not a hot-loop); #801 should treat a populated per-track identity as the resolved signal so they aren't perpetually re-asked.

## Drizzle-kit "applied-hashes.json data is malformed" quirk

`npm run drizzle:generate` emits a `applied-hashes.json data is malformed` warning before the migration generation step. This is drizzle-kit's `prepareOutFolder` treating every `.json` file under `meta/` as a snapshot candidate; `applied-hashes.json` is a Backend-Service-only ledger (see Check 7 in `scripts/validate-migrations.mjs`) and doesn't match the snapshot shape. The warning is non-blocking.

**Workaround when the warning blocks generation locally** (it shouldn't, but if drizzle-kit's behavior tightens in a future bump): `mv shared/database/src/migrations/meta/applied-hashes.json /tmp/ ; npm run drizzle:generate ; mv /tmp/applied-hashes.json shared/database/src/migrations/meta/`. Then `npm run drizzle:freeze-hashes` to add the new migration's hash, and `npm run lint:migrations` to confirm.

Don't move the file aside as part of the migration PR — restore it before commit.

## DDL-only migrations via `drizzle-kit generate --custom`

Some DDL doesn't correspond to any Drizzle-modeled schema change — `COMMENT ON COLUMN` is the case that motivated this section (migration `0157`), but a `GRANT` or an extension install qualifies the same way. Plain `npm run drizzle:generate` emits nothing on a no-diff schema, since a column comment (or any of these) isn't part of the Drizzle schema model, so there's no diff to detect. The escape hatch is an empty custom migration:

1. `npx drizzle-kit generate --config drizzle.config.ts --custom --name <slug>` creates the empty `.sql` file and appends the journal entry. It needs `DB_*` env vars set for `drizzle.config.ts` to evaluate, but `--custom` never opens a connection — dummy values work fine, e.g. `DB_USERNAME=x DB_HOST=localhost DB_PORT=5432 DB_NAME=x`.

2. **The `applied-hashes.json data is malformed` notice is NOT optional to work around under `--custom`**, unlike the plain-`generate` quirk documented above, where the malformed-file warning is non-blocking and the move-aside workaround is a just-in-case measure "if drizzle-kit's behavior tightens in a future bump" — true for a real schema diff, where the malformed file is merely skipped. Under `--custom` on drizzle-kit 0.31.10 it already fails every time: `preparePrevSnapshot` reads `snapshots[snapshots.length - 1]` off `meta/`'s directory listing sorted _alphabetically_, not by migration index, so `applied-hashes.json` (sorting after every `NNNN_snapshot.json`, since `'a'` > `'0'`-`'9'`) is picked as "the previous snapshot", fails `pgSchema.parse`, and drizzle-kit silently exits 0 having written nothing — no error, no `.sql`, no journal entry, just the malformed notice and then silence. The same move-aside workaround fixes it (step 6 below regenerates the moved file from scratch anyway, so the temporary absence is harmless) — it just isn't optional here:

   ```bash
   mv shared/database/src/migrations/meta/applied-hashes.json /tmp/
   npx drizzle-kit generate --config drizzle.config.ts --custom --name <slug>
   mv /tmp/applied-hashes.json shared/database/src/migrations/meta/
   ```

3. Hand-write the DDL below the placeholder line drizzle-kit leaves (`-- Custom SQL migration file, put your code below! --`), and prepend a header comment explaining the migration's purpose. `COMMENT ON COLUMN` is idempotent (last write wins, no error on re-apply), so no `IF NOT EXISTS`-style guard is needed — and see the `dev-prod-pg-version-skew` rule above: this syntax is plain SQL, unchanged since Postgres 7.2, so it's PG14-safe by construction, no release-notes check required.

   **Qualify each object with the right schema — this repo uses two.** Hand-written DDL gets no help from drizzle-kit here, and `wxyc_schema` is the wrong guess for the auth tables: `auth_user` (and every other better-auth table) is declared with a bare `pgTable(...)` in `schema.ts`, so it lives in `public`, while `flowsheet`, `shows`, and the rest are `wxyc_schema.table(...)`. Drizzle's own generated SQL shows both forms side by side — see `0021`'s `REFERENCES "public"."auth_user"("id")` against its `ALTER TABLE "wxyc_schema"."shows"`. Guessing wrong fails at apply time with `42P01 relation "..." does not exist`, which `lint:migrations` cannot catch: it validates the journal, not the DDL. Only actually applying the chain does — `npm run db:start`, or `node dev_env/init-db.mjs` against a throwaway Postgres.

4. If `meta/<idx>_snapshot.json` wasn't written (only possible if step 2's workaround wasn't applied), duplicate the predecessor's snapshot under the new index — a no-Drizzle-diff migration's snapshot is byte-for-byte identical to its predecessor's, since nothing in the Drizzle-modeled schema changed.

5. Journal `when`: drizzle-kit auto-stamps `Date.now()` on the new entry. Diff it against the previous entry's `when` (the `hand-edit-when` rule above) rather than assuming — hand-bump to `previous.when + 1` only if it isn't already strictly greater.

6. `npm run drizzle:freeze-hashes` (regenerates `meta/applied-hashes.json` from every `.sql` file's current content), then `npm run lint:migrations` must be green before committing.

## Journal idx vs filename idx (post-2026-06-14 invariant)

From journal idx 47 onward, the journal `idx` field is one greater than the leading number in the corresponding filename. `_journal.json` historically had two entries at idx 47 (`0046_cdc_notify_triggers` and `0047_replica-identity-for-pkless-tables`); #1131 / PR #1415 broke the tie by shifting every entry from idx 47 onward by +1. Filenames were not renamed, so the relationship is now:

| Filename                  | Journal idx    |
| ------------------------- | -------------- |
| `0000_*.sql`–`0045_*.sql` | 0–46 (matches) |
| `0046_*.sql`              | 47             |
| `0047_*.sql`              | 48             |
| ...                       | ...            |
| `0099_*.sql`              | 100            |

This is benign at runtime: Drizzle's runtime migrator identifies a migration by its `tag` (filename), not by `idx`. The mismatch only matters when reasoning about a journal entry without its filename next to it — `idx N` is not the same as filename `N` from this point onward. New migrations continue to follow the `previous_entry.when + 1` recipe and `drizzle-kit generate` auto-allocates the next idx; no special handling required.

Don't "fix" the off-by-one without good reason. Renumbering ripples through every per-step `meta/<idx>_snapshot.json` (PR #1415 itself was a ~4800-line diff for this reason).

## Rule annotation convention

Each rule above is preceded by a `<!-- @rule -->` marker. Fields:

- `id` — short kebab-case identifier; must be unique across all `docs/*.md` rules.
- `enforced-by` — file path + check name (e.g. `scripts/validate-migrations.mjs:check-1`), or `none` when the rule lives on author discipline.
- `added` — ISO date the rule was added.
- `incidents` — comma-separated list of incident issues (`#NNN`). Optional.
- `review-after` — optional ISO date. When set and reached, `scripts/check-doc-rules.mjs` flags the rule for review.

`scripts/check-doc-rules.mjs` parses these markers and emits warnings for:

- rules whose `enforced-by` is `none` and whose `added` date is more than 180 days ago — consider promoting the rule to a check, or removing it if it's been internalized
- rules whose `enforced-by` is non-`none` AND whose prose body exceeds ~600 chars — the check now carries the load; the prose can collapse to a 1-line pointer
- rules whose `review-after` date has passed

Run on demand: `npm run check:doc-rules`. Also runs in `.husky/pre-push` (warn-only — never blocks).

The intent is a forcing function for the question this codebase keeps re-asking: when is an incident-anchored rule ready to compress? Once enforcement exists in a script or CI check, the prose stops being load-bearing and becomes commentary — the script tells you when the moment has arrived.
